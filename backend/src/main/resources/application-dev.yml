spring:
  datasource:
    url: jdbc:mysql://localhost:3306/skillbridge_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: bhanu
    password: bhanu123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: SkillBridgeDevPool
      minimum-idle: 2
      maximum-pool-size: 5

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true